#include "../includes/autotest.h"
// #include "../includes/array_sequence.h"


int main()
{
    // Array_sequence as;

    // as.print();
    
    // as.push_front(1);
    // as.push_front(2);
    // as.push_front(3);
    // as.push_front(4);
    
    // as.print();

    // as.push_back(5);
    // as.push_back(6);
    // as.push_back(7);
    // as.push_back(8);
    
    // as.print();

    // as.insert(9, 100);
    // as.insert(9, 90);
    // as.insert(1, 0);
    // as.insert(5, 50);
    
    // as.print();

    // as.remove(12);
    // as.remove(1);
    // as.remove(5);

    // as.print();

    // auto a = as.find(50);
    // auto b = as.find(100);
    // auto c = as.find(30);

    // if (a) std::cout << "find 50:  " << *a << std::endl;
    // else   std::cout << "find 50:  none" << std::endl;
    // if (b) std::cout << "find 100: " << *b << std::endl;
    // else   std::cout << "find 100: none" << std::endl;
    // if (c) std::cout << "find 30:  " << *c << std::endl;
    // else   std::cout << "find 30:  none" << std::endl;

    start_test();

    return 0;
}