#include "../includes/autotest.h"


int main()
{
    // Sequence_ADT sec;

    // sec.insert(1, 10);
    // sec.insert(2, 20);
    // sec.insert(3, 30);
    // sec.insert(4, 40);

    // sec.print();

    // sec.remove(2);

    // sec.print();

    start_test();

    return 0;
}