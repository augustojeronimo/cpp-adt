#include "../includes/autotest.h"
// #include "../includes/set.h"

int main()
{
    // Autotest
    start_test();

    // Set A;

    // A.add(1);
    // A.add(2);
    // A.add(3);
    // A.add(4);

    // A.remove(4);

    // Set C(A);

    // C.remove(2);

    // A.print();
    // C.print();

    // (A&C).print();


    return 0;
}